<%= simple_form_for @feedback, :html => { :class => 'form-horizontal' } do |f| %>
  
  <%= f.input :name %>
  <%= f.input :comment%>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                feedbacks_path, :class => 'btn' %>
  </div>
<% end %>
